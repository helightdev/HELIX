# ReferenceSpanAction<T, R> (/reference/HELIX.ReferenceSpanAction-2)

# ReferenceSpanAction<T, R>

```
public delegate void ReferenceSpanAction<T, R>(Span<T> span, ref R reference) where T : unmanaged
```