---
title: Context
description: Developer map of HELIX component discovery, dependency scopes, lifecycle, events, and mixins.
icon: Blocks
---

Context is HELIX's application framework for components and services. It discovers generated
component registrations, builds dependency graphs, activates components inside managed scopes,
injects and publishes values, runs lifecycle methods, and tears scopes down deterministically.

Application code declares behavior with attributes. Mixin expressions translate those attributes
into registration metadata and generated lifecycle code, avoiding broad runtime reflection.

## Architecture

<Cards>
  <Card title="Components & Loading" description="Registration, activation, phases, graph scheduling, loading order, optional components, and failures." href="/docs/context/components-and-loading" />
  <Card title="Scopes & Dependencies" description="Scope hierarchy, rules, resolution, injection, publications, resources, and owned values." href="/docs/context/scopes-and-dependencies" />
  <Card title="Lifecycle" description="Generated lifecycle targets, callback ordering, startup, rollback, and deterministic teardown." href="/docs/context/lifecycle" />
  <Card title="Events" description="Typed reactors, priorities, generated handlers, registration lifetime, and asynchronous chains." href="/docs/context/events" />
  <Card title="Tickers & Auto Dispose" description="Generated scheduling, the complete time-expression syntax, conditions, async exclusion, and field disposal." href="/docs/context/tickers-and-auto-dispose" />
  <Card title="Mixin Expressions" description="The compile-time language underlying Context attributes and lifecycle contributions." href="/docs/context/mixin-expressions" />
</Cards>

## Runtime objects

| Object | Responsibility |
|---|---|
| <FREntityCodeLink uid="ManagedContainer" /> | Owns registrations, the registrar/application roots, active scopes, rules, handlers, and loaders. |
| <FREntityCodeLink uid="ManagedScope" /> | Owns local bindings, loaded components, child scopes, cancellation, and resources. |
| `ComponentRegistration` | Describes activation, scope, keys, dependencies, publications, phase, order, and conditions. |
| <FREntityCodeLink uid="RuntimeComponentData" /> | Connects a generated component instance to its container and scope. |
| `ScopeLoader` | Executes scripted dependencies and the component dependency graph for one scope start. |

## Typical component

```csharp
[Component(typeof(ApplicationScope), order: 10)]
public partial class WelcomeService {
  [Inject]
  public GameSettings settings;

  [EventHandler(EventPriority.Late)]
  private void OnSessionStarted(SessionStartedEvent evt) { }

  [Ticker("5 seconds")]
  private void Refresh() { }

  [AutoDispose]
  private IDisposable subscription;

  [MixinMethod]
  private void OnInit() { }
}
```

The generated implementation registers the component and its dependency/event metadata, injects
`settings`, contributes callbacks to lifecycle targets, schedules `Refresh`, tracks the event
registration, and disposes both generated registrations and the annotated field during unload.

## Context versus Compose context

Context is the application and scene service system. Compose context is tree-local reactive data
addressed by `ContextKey<T>` and observed by composition boundaries. A Compose boundary may use
both, but the two systems have different ownership, lookup, and update semantics.

<Cards>
  <FREntityCardLink uid="ManagedContainer" />
  <FREntityCardLink uid="ManagedScope" />
  <FREntityCardLink uid="ComponentAttribute" />
  <FREntityCardLink uid="IComponent" />
</Cards>
